<?xml version="1.0" encoding="utf-8"?>

<layout>

    <data></data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:orientation="vertical"
        android:paddingTop="@dimen/activity_vertical_margin"
        tools:context="com.example.priya.musicplayer.activity.LandingActivity">

        <Button
            android:layout_width="200dp"
            android:layout_height="50dp"
            android:onClick="onMusicListButtonClick"
            android:text="@string/open_music_list"/>

        <Button
            android:id="@+id/hello_world"
            android:layout_width="200dp"
            android:layout_height="50dp"
            android:onClick="onHelloWorldButtonClick"
            android:text="@string/open_hello_world" />

    </LinearLayout>
</layout>